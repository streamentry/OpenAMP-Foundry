# Screenshot and Image Policy

- Avoid screenshots — use Mermaid diagrams instead
- If a screenshot is necessary, use PNG format
- Keep images under 500 KB
- Do not include screenshots of code — use code blocks instead
- Do not include screenshots of terminal output — use text blocks instead
- Alt text is required for all images
- Store images in `docs/assets/`
