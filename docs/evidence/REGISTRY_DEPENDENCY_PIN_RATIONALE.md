# Dependency Pin Rationale Registry

Why specific dependency versions are pinned.

| Dependency | Pinned Version | Reason |
|------------|:--------------:|--------|
| — | — | No dependencies are currently pinned |

## Policy
- Pin versions only when necessary (incompatibility, security fix).
- Document the reason for each pin.
- Review pins quarterly and remove if no longer needed.
- Use range constraints (`>=`) rather than exact pins where possible.
