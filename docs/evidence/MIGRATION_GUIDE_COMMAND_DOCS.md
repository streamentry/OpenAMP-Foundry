# Migration Guide: Command Docs

When CLI commands change, update:

1. `docs/getting-started/COMMAND_SURFACE.md` for command workflows.
2. `docs/evidence/EXIT_CODE_REGISTRY.md` if exit codes changed.
3. Any runbooks that reference the old command.
4. QUICKSTART.md if the change affects demo commands.
