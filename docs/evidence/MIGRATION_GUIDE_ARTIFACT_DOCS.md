# Migration Guide: Artifact Docs

When a new artifact is added to the pipeline:

1. Add an entry in `docs/evidence/ARTIFACT_MAP.md`.
2. If the artifact has a schema, add it to `schemas/`.
3. Add a concept card if the artifact type is new.
4. Update the reproducibility report script if needed.
