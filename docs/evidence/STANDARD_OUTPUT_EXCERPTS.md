# Command Output Excerpt Standard

- Show only the relevant portion of command output, not the full output
- Use `...` to indicate omitted content
- Label the excerpt with the command that produced it
- If the output is machine-readable (JSON), show it as a code block
- If the output is human-readable, show it as a text block
- Annotate important parts with comments where helpful
