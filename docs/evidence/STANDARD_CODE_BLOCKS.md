# Code Block Standard for Commands

- Use `bash` language tag for shell commands
- Use `python` language tag for Python examples
- Use `json` for JSON examples
- Use `text` for output that is not code
- Every command example should be runnable from a clean checkout
- Include the expected output format, not the exact output
- Use `$` prefix for commands, no `$` prefix for output
