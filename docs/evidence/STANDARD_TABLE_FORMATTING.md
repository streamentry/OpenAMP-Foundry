# Table Formatting Standard

- Use pipe tables (`| col | col |`)
- Include a separator row after the header
- Align columns with `:---` (left), `:---:` (center), `---:` (right)
- No empty cells — use `—` or `N/A` instead
- Keep tables under 8 columns wide
- Break long tables into multiple smaller tables where possible
